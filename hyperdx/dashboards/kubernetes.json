{
  "name": "ClickStack - Kubernetes",
  "tags": [
    "clickstack-templates",
    "kubernetes",
    "tmpl:kubernetes",
    "control-plane"
  ],
  "filters": [
    {
      "type": "QUERY_EXPRESSION",
      "name": "Namespace",
      "expression": "ResourceAttributes['k8s.namespace.name']",
      "sourceId": "{{METRICS_SOURCE_ID}}",
      "sourceMetricType": "gauge",
      "whereLanguage": "sql"
    },
    {
      "type": "QUERY_EXPRESSION",
      "name": "Event Namespace",
      "expression": "JSONExtractString(Body, 'object', 'regarding', 'namespace')",
      "sourceId": "{{LOGS_SOURCE_ID}}",
      "whereLanguage": "sql"
    }
  ],
  "tiles": [
    {
      "name": "",
      "x": 0,
      "y": 0,
      "w": 24,
      "h": 5,
      "config": {
        "displayType": "markdown",
        "markdown": "### Kubernetes Overview\nCluster, nodes, namespaces, pods/workloads, containers, resource utilization, and events/issues. Filter by Namespace and use the inventory and troubleshooting tables to move from cluster health to the affected resource."
      }
    },
    {
      "name": "",
      "x": 0,
      "y": 5,
      "w": 24,
      "h": 6,
      "config": {
        "displayType": "markdown",
        "markdown": "## 1. Cluster Overview\nNode readiness and per-node CPU, memory, and filesystem usage against capacity."
      }
    },
    {
      "name": "Kubernetes nodes ready %",
      "x": 0,
      "y": 11,
      "w": 8,
      "h": 6,
      "config": {
        "displayType": "number",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT if(total = 0, 0, ready / total) AS \"Nodes ready\" FROM (\n  SELECT countIf(ready = 1) AS ready, count() AS total FROM (\n    SELECT ResourceAttributes['k8s.node.name'] AS node, argMax(Value, TimeUnix) AS ready\n    FROM {{METRICS_SCHEMA}}.otel_metrics_gauge\n    WHERE TimeUnix >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64})\n      AND TimeUnix <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64})\n      AND MetricName = 'k8s.node.condition_ready'\n    GROUP BY node\n  )\n)",
        "numberFormat": {
          "output": "percent",
          "mantissa": 2
        },
        "colorRules": [
          {
            "operator": "lt",
            "value": 1,
            "color": "chart-warning",
            "label": "Node(s) not ready"
          },
          {
            "operator": "lt",
            "value": 0.8,
            "color": "chart-error",
            "label": "Multiple nodes down"
          }
        ]
      }
    },
    {
      "name": "Host CPU utilization %",
      "x": 8,
      "y": 11,
      "w": 16,
      "h": 6,
      "config": {
        "displayType": "line",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT ts, host, avg(busy) AS \"CPU\" FROM (\n  SELECT toStartOfInterval(TimeUnix, toIntervalSecond(greatest(toInt64(1), intDiv({endDateMilliseconds:Int64} - {startDateMilliseconds:Int64}, toInt64(120000))))) AS ts,\n         ResourceAttributes['host.name'] AS host,\n         Attributes['cpu'] AS cpu,\n         TimeUnix AS t,\n         sumIf(Value, Attributes['state'] != 'idle') AS busy\n  FROM {{METRICS_SCHEMA}}.otel_metrics_gauge\n  WHERE TimeUnix >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64})\n    AND TimeUnix <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64})\n      AND MetricName = 'system.cpu.utilization'\n  GROUP BY ts, host, cpu, t\n) GROUP BY ts, host ORDER BY ts",
        "numberFormat": {
          "output": "percent",
          "mantissa": 2
        }
      }
    },
    {
      "name": "Host memory utilization %",
      "x": 0,
      "y": 17,
      "w": 24,
      "h": 6,
      "config": {
        "displayType": "line",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "numberFormat": {
          "output": "percent",
          "mantissa": 2
        },
        "sqlTemplate": "SELECT toStartOfInterval(TimeUnix, toIntervalSecond(greatest(toInt64(1), intDiv({endDateMilliseconds:Int64} - {startDateMilliseconds:Int64}, toInt64(120000))))) AS ts,\n       ResourceAttributes['host.name'] AS host,\n       avgIf(Value, Attributes['state'] = 'used') AS \"Memory\"\nFROM {{METRICS_SCHEMA}}.otel_metrics_gauge\nWHERE TimeUnix >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64})\n    AND TimeUnix <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64})\n    AND MetricName = 'system.memory.utilization'\nGROUP BY ts, host ORDER BY ts"
      }
    },
    {
      "name": "Filesystem used % per volume",
      "x": 0,
      "y": 23,
      "w": 24,
      "h": 6,
      "config": {
        "displayType": "line",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "numberFormat": {
          "output": "percent",
          "mantissa": 2
        },
        "sqlTemplate": "SELECT ts, volume, used / nullIf(total, 0) AS \"Filesystem\" FROM (\n  SELECT toStartOfInterval(TimeUnix, toIntervalSecond(greatest(toInt64(1), intDiv({endDateMilliseconds:Int64} - {startDateMilliseconds:Int64}, toInt64(120000))))) AS ts,\n    concat(ResourceAttributes['host.name'], ' ', Attributes['mountpoint']) AS volume,\n    sumIf(Value, Attributes['state'] = 'used') AS used,\n    sum(Value) AS total\n  FROM {{METRICS_SCHEMA}}.otel_metrics_sum\n  WHERE TimeUnix >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64})\n    AND TimeUnix <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64})\n    AND MetricName = 'system.filesystem.usage'\n  GROUP BY ts, volume\n) WHERE total > 0 ORDER BY ts"
      }
    },
    {
      "name": "Nodes - status & uptime",
      "x": 0,
      "y": 29,
      "w": 24,
      "h": 6,
      "config": {
        "displayType": "table",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "WITH g AS (\n  SELECT ResourceAttributes['k8s.node.name'] AS node,\n    argMax(Value, TimeUnix) AS ready\n  FROM {{METRICS_SCHEMA}}.otel_metrics_gauge\n  WHERE TimeUnix > now() - INTERVAL 1 HOUR\n    AND MetricName = 'k8s.node.condition_ready'\n  GROUP BY node\n),\ns AS (\n  SELECT ResourceAttributes['k8s.node.name'] AS node, argMax(Value, TimeUnix) AS uptime\n  FROM {{METRICS_SCHEMA}}.otel_metrics_sum\n  WHERE TimeUnix > now() - INTERVAL 1 HOUR AND MetricName = 'k8s.node.uptime'\n  GROUP BY node\n)\nSELECT g.node AS Node,\n  if(g.ready = 1, 'Ready', 'Not Ready') AS Status,\n  formatReadableTimeDelta(toUInt64(s.uptime)) AS Uptime\nFROM g LEFT JOIN s USING (node)\nORDER BY Status DESC, s.uptime ASC"
      }
    },
    {
      "name": "",
      "x": 0,
      "y": 36,
      "w": 24,
      "h": 6,
      "config": {
        "displayType": "markdown",
        "markdown": "## 2. Namespace Overview\nResource consumption broken down by namespace."
      }
    },
    {
      "name": "Namespace CPU usage (cores)",
      "x": 0,
      "y": 42,
      "w": 12,
      "h": 6,
      "config": {
        "displayType": "line",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT ts, ns, sum(pod_cpu) AS \"CPU (cores)\" FROM (\n  SELECT toStartOfInterval(TimeUnix, toIntervalSecond(greatest(toInt64(1), intDiv({endDateMilliseconds:Int64} - {startDateMilliseconds:Int64}, toInt64(120000))))) AS ts,\n         ResourceAttributes['k8s.namespace.name'] AS ns,\n         ResourceAttributes['k8s.pod.name'] AS pod,\n         avg(Value) AS pod_cpu\n  FROM {{METRICS_SCHEMA}}.otel_metrics_gauge\n  WHERE TimeUnix >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64})\n    AND TimeUnix <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64})\n    AND MetricName = 'k8s.pod.cpu.usage' AND $__filters\n  GROUP BY ts, ns, pod\n)\nGROUP BY ts, ns\nORDER BY ts",
        "numberFormat": {
          "output": "number",
          "mantissa": 2
        }
      }
    },
    {
      "name": "Namespace memory usage",
      "x": 12,
      "y": 42,
      "w": 12,
      "h": 6,
      "config": {
        "displayType": "line",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "numberFormat": {
          "output": "byte",
          "numericUnit": "bytes_iec",
          "mantissa": 2
        },
        "sqlTemplate": "SELECT ts, ns, sum(pod_mem) AS \"Memory\" FROM (\n  SELECT toStartOfInterval(TimeUnix, toIntervalSecond(greatest(toInt64(1), intDiv({endDateMilliseconds:Int64} - {startDateMilliseconds:Int64}, toInt64(120000))))) AS ts,\n         ResourceAttributes['k8s.namespace.name'] AS ns,\n         ResourceAttributes['k8s.pod.name'] AS pod,\n         avg(Value) AS pod_mem\n  FROM {{METRICS_SCHEMA}}.otel_metrics_gauge\n  WHERE TimeUnix >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64})\n    AND TimeUnix <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64})\n    AND MetricName = 'k8s.pod.memory.usage' AND $__filters\n  GROUP BY ts, ns, pod\n)\nGROUP BY ts, ns\nORDER BY ts"
      }
    },
    {
      "name": "Namespaces - phase, CPU, memory",
      "x": 0,
      "y": 48,
      "w": 24,
      "h": 6,
      "config": {
        "displayType": "table",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "WITH pods AS (\n  SELECT ResourceAttributes['k8s.namespace.name'] AS ns,\n    ResourceAttributes['k8s.pod.name'] AS pod,\n    argMaxIf(Value, TimeUnix, MetricName = 'k8s.pod.cpu.usage') AS cpu,\n    argMaxIf(Value, TimeUnix, MetricName = 'k8s.pod.memory.usage') AS mem\n  FROM {{METRICS_SCHEMA}}.otel_metrics_gauge\n  WHERE TimeUnix > now() - INTERVAL 1 HOUR\n    AND MetricName IN ('k8s.pod.cpu.usage', 'k8s.pod.memory.usage')\n    AND $__filters\n  GROUP BY ns, pod\n),\nagg AS ( SELECT ns, sum(cpu) AS cpu, sum(mem) AS mem FROM pods GROUP BY ns ),\nph AS (\n  SELECT ResourceAttributes['k8s.namespace.name'] AS ns, argMax(Value, TimeUnix) AS phase\n  FROM {{METRICS_SCHEMA}}.otel_metrics_gauge\n  WHERE TimeUnix > now() - INTERVAL 1 HOUR AND MetricName = 'k8s.namespace.phase'\n  GROUP BY ns\n)\nSELECT agg.ns AS Namespace,\n  multiIf(ph.phase = 1, 'Active', ph.phase = 2, 'Terminating', 'Unknown') AS Phase,\n  round(agg.cpu, 2) AS \"CPU (cores)\",\n  formatReadableSize(agg.mem) AS Memory\nFROM agg LEFT JOIN ph USING (ns)\nORDER BY agg.cpu DESC"
      }
    },
    {
      "name": "",
      "x": 0,
      "y": 55,
      "w": 24,
      "h": 6,
      "config": {
        "displayType": "markdown",
        "markdown": "## 3. Workload Health\nDeployment availability, pod phases, restarts, and the pods using the most resources."
      }
    },
    {
      "name": "Deployment availability (ready / desired)",
      "x": 0,
      "y": 61,
      "w": 12,
      "h": 6,
      "config": {
        "displayType": "line",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "asRatio": true,
        "numberFormat": {
          "output": "percent",
          "mantissa": 2
        },
        "groupBy": "concat(ResourceAttributes['k8s.namespace.name'], '/', ResourceAttributes['k8s.deployment.name'])",
        "select": [
          {
            "aggFn": "last_value",
            "valueExpression": "Value",
            "alias": "available",
            "metricName": "k8s.deployment.available",
            "metricType": "gauge",
            "where": "",
            "whereLanguage": "sql"
          },
          {
            "aggFn": "last_value",
            "valueExpression": "Value",
            "alias": "desired",
            "metricName": "k8s.deployment.desired",
            "metricType": "gauge",
            "where": "",
            "whereLanguage": "sql"
          }
        ]
      }
    },
    {
      "name": "Pods by phase (count)",
      "x": 12,
      "y": 61,
      "w": 12,
      "h": 6,
      "config": {
        "displayType": "table",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT multiIf(phase = 1, 'Pending', phase = 2, 'Running', phase = 3, 'Succeeded', phase = 4, 'Failed', 'Unknown') AS \"Phase\", count() AS \"Pods\" FROM (\n  SELECT ResourceAttributes['k8s.pod.name'] AS pod, argMax(Value, TimeUnix) AS phase\n  FROM {{METRICS_SCHEMA}}.otel_metrics_gauge\n  WHERE TimeUnix >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64})\n    AND TimeUnix <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64})\n    AND MetricName = 'k8s.pod.phase' AND $__filters\n  GROUP BY pod\n)\nGROUP BY phase\nORDER BY count() DESC"
      }
    },
    {
      "name": "Pods not Running",
      "x": 0,
      "y": 67,
      "w": 12,
      "h": 5,
      "config": {
        "displayType": "number",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT countIf(phase NOT IN (2, 3)) AS \"Not running\" FROM (\n  SELECT ResourceAttributes['k8s.pod.name'] AS pod, argMax(Value, TimeUnix) AS phase\n  FROM {{METRICS_SCHEMA}}.otel_metrics_gauge\n  WHERE TimeUnix >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64})\n    AND TimeUnix <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64})\n    AND MetricName = 'k8s.pod.phase' AND $__filters\n  GROUP BY pod\n)",
        "colorRules": [
          {
            "operator": "gt",
            "value": 0,
            "color": "chart-warning",
            "label": "Pods not Running"
          }
        ],
        "numberFormat": {
          "output": "number",
          "mantissa": 0
        }
      }
    },
    {
      "name": "Container restarts (selected range)",
      "x": 12,
      "y": 67,
      "w": 12,
      "h": 5,
      "config": {
        "displayType": "number",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT sum(d) AS \"New restarts\" FROM (\n  SELECT greatest(max(Value) - min(Value), 0) AS d\n  FROM {{METRICS_SCHEMA}}.otel_metrics_gauge\n  WHERE TimeUnix >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64})\n    AND TimeUnix <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64})\n    AND MetricName = 'k8s.container.restarts' AND $__filters\n  GROUP BY ResourceAttributes['k8s.namespace.name'],\n           ResourceAttributes['k8s.pod.name'],\n           ResourceAttributes['k8s.container.name']\n)",
        "colorRules": [
          {
            "operator": "gt",
            "value": 0,
            "color": "chart-warning",
            "label": "Restarts happening"
          }
        ],
        "numberFormat": {
          "output": "number",
          "mantissa": 0
        }
      }
    },
    {
      "name": "Pods - status & resources",
      "x": 0,
      "y": 72,
      "w": 24,
      "h": 6,
      "config": {
        "displayType": "table",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "WITH g AS (\n  SELECT ResourceAttributes['k8s.pod.name'] AS pod,\n    any(ResourceAttributes['k8s.namespace.name']) AS ns,\n    argMaxIf(Value, TimeUnix, MetricName = 'k8s.pod.phase') AS phase,\n    argMaxIf(Value, TimeUnix, MetricName = 'k8s.pod.cpu_limit_utilization') AS cpu_lim,\n    argMaxIf(Value, TimeUnix, MetricName = 'k8s.pod.memory_limit_utilization') AS mem_lim,\n    argMaxIf(Value, TimeUnix, MetricName = 'k8s.pod.memory.usage') AS mem,\n    maxIf(Value, MetricName = 'k8s.container.restarts') AS restarts\n  FROM {{METRICS_SCHEMA}}.otel_metrics_gauge\n  WHERE TimeUnix > now() - INTERVAL 1 HOUR\n    AND MetricName IN ('k8s.pod.phase', 'k8s.pod.cpu_limit_utilization', 'k8s.pod.memory_limit_utilization', 'k8s.pod.memory.usage', 'k8s.container.restarts')\n    AND $__filters\n  GROUP BY pod\n),\ns AS (\n  SELECT ResourceAttributes['k8s.pod.name'] AS pod, argMax(Value, TimeUnix) AS uptime\n  FROM {{METRICS_SCHEMA}}.otel_metrics_sum\n  WHERE TimeUnix > now() - INTERVAL 1 HOUR AND MetricName = 'k8s.pod.uptime'\n  GROUP BY pod\n)\nSELECT g.ns AS Namespace,\n  g.pod AS Pod,\n  multiIf(g.phase = 1, 'Pending', g.phase = 2, 'Running', g.phase = 3, 'Succeeded', g.phase = 4, 'Failed', 'Unknown') AS Status,\n  if(isNaN(g.cpu_lim), '-', concat(toString(round(g.cpu_lim * 100, 2)), '%')) AS \"CPU/limit\",\n  if(isNaN(g.mem_lim), '-', concat(toString(round(g.mem_lim * 100, 2)), '%')) AS \"Mem/limit\",\n  formatReadableSize(g.mem) AS Memory,\n  formatReadableTimeDelta(toUInt64(s.uptime)) AS Age,\n  toUInt64(g.restarts) AS Restarts\nFROM g LEFT JOIN s USING (pod)\nORDER BY g.restarts DESC, g.cpu_lim DESC\nLIMIT 100"
      }
    },
    {
      "name": "Top pods by restarts",
      "x": 0,
      "y": 78,
      "w": 24,
      "h": 6,
      "config": {
        "displayType": "table",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT ns AS \"Namespace\", pod AS \"Pod\", toUInt64(restarts) AS \"Restarts\" FROM (\n  SELECT ResourceAttributes['k8s.namespace.name'] AS ns,\n         ResourceAttributes['k8s.pod.name'] AS pod,\n         argMax(Value, TimeUnix) AS restarts\n  FROM {{METRICS_SCHEMA}}.otel_metrics_gauge\n  WHERE TimeUnix >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64})\n    AND TimeUnix <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64})\n    AND MetricName = 'k8s.container.restarts' AND $__filters\n  GROUP BY ns, pod\n)\nWHERE restarts > 0\nORDER BY restarts DESC\nLIMIT 50"
      }
    },
    {
      "name": "",
      "x": 0,
      "y": 85,
      "w": 24,
      "h": 6,
      "config": {
        "displayType": "markdown",
        "markdown": "## 4. Cluster Utilization\nHow close pods and containers are running to their configured CPU and memory limits."
      }
    },
    {
      "name": "Pod CPU vs limit %",
      "x": 0,
      "y": 91,
      "w": 12,
      "h": 6,
      "config": {
        "displayType": "line",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "numberFormat": {
          "output": "percent",
          "mantissa": 2
        },
        "groupBy": "ResourceAttributes['k8s.pod.name']",
        "select": [
          {
            "aggFn": "max",
            "valueExpression": "Value",
            "alias": "cpu vs limit",
            "metricName": "k8s.pod.cpu_limit_utilization",
            "metricType": "gauge",
            "where": "",
            "whereLanguage": "sql"
          }
        ]
      }
    },
    {
      "name": "Pod memory vs limit %",
      "x": 12,
      "y": 91,
      "w": 12,
      "h": 6,
      "config": {
        "displayType": "line",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "numberFormat": {
          "output": "percent",
          "mantissa": 2
        },
        "groupBy": "ResourceAttributes['k8s.pod.name']",
        "select": [
          {
            "aggFn": "max",
            "valueExpression": "Value",
            "alias": "mem vs limit",
            "metricName": "k8s.pod.memory_limit_utilization",
            "metricType": "gauge",
            "where": "",
            "whereLanguage": "sql"
          }
        ]
      }
    },
    {
      "name": "Container CPU vs limit %",
      "x": 0,
      "y": 97,
      "w": 12,
      "h": 6,
      "config": {
        "displayType": "line",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "numberFormat": {
          "output": "percent",
          "mantissa": 2
        },
        "sqlTemplate": "SELECT toStartOfInterval(TimeUnix, toIntervalSecond(greatest(toInt64(1), intDiv({endDateMilliseconds:Int64} - {startDateMilliseconds:Int64}, toInt64(120000))))) AS ts,\n       concat(ResourceAttributes['k8s.namespace.name'], '/', ResourceAttributes['k8s.pod.name'], '/', ResourceAttributes['k8s.container.name']) AS container,\n       avg(Value) AS \"CPU vs limit\"\nFROM {{METRICS_SCHEMA}}.otel_metrics_gauge\nWHERE TimeUnix >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64})\n    AND TimeUnix <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64})\n    AND MetricName = 'k8s.container.cpu_limit_utilization' AND $__filters\nGROUP BY ts, container\nORDER BY ts"
      }
    },
    {
      "name": "Container memory vs limit %",
      "x": 12,
      "y": 97,
      "w": 12,
      "h": 6,
      "config": {
        "displayType": "line",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "numberFormat": {
          "output": "percent",
          "mantissa": 2
        },
        "sqlTemplate": "SELECT toStartOfInterval(TimeUnix, toIntervalSecond(greatest(toInt64(1), intDiv({endDateMilliseconds:Int64} - {startDateMilliseconds:Int64}, toInt64(120000))))) AS ts,\n       concat(ResourceAttributes['k8s.namespace.name'], '/', ResourceAttributes['k8s.pod.name'], '/', ResourceAttributes['k8s.container.name']) AS container,\n       avg(Value) AS \"Mem vs limit\"\nFROM {{METRICS_SCHEMA}}.otel_metrics_gauge\nWHERE TimeUnix >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64})\n    AND TimeUnix <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64})\n    AND MetricName = 'k8s.container.memory_limit_utilization' AND $__filters\nGROUP BY ts, container\nORDER BY ts"
      }
    },
    {
      "name": "Host memory saturation %",
      "x": 0,
      "y": 103,
      "w": 24,
      "h": 5,
      "config": {
        "displayType": "number",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "numberFormat": {
          "output": "percent",
          "mantissa": 2
        },
        "sqlTemplate": "SELECT max(sat) AS \"Host mem saturation\" FROM (\n  SELECT ResourceAttributes['host.name'] AS host,\n         argMaxIf(Value, TimeUnix, Attributes['state'] = 'used') AS sat\n  FROM {{METRICS_SCHEMA}}.otel_metrics_gauge\n  WHERE TimeUnix >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64})\n    AND TimeUnix <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64})\n    AND MetricName = 'system.memory.utilization'\n  GROUP BY host\n)",
        "colorRules": [
          {
            "operator": "gt",
            "value": 0.85,
            "color": "chart-warning",
            "label": "High"
          },
          {
            "operator": "gt",
            "value": 0.95,
            "color": "chart-error",
            "label": "Critical"
          }
        ]
      }
    },
    {
      "name": "Containers - utilization vs limit / request",
      "x": 0,
      "y": 108,
      "w": 24,
      "h": 7,
      "config": {
        "displayType": "table",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "WITH g AS (\n  SELECT ResourceAttributes['k8s.namespace.name'] AS ns,\n    ResourceAttributes['k8s.pod.name'] AS pod,\n    ResourceAttributes['k8s.container.name'] AS container,\n    argMaxIf(Value, TimeUnix, MetricName = 'k8s.container.cpu_limit_utilization') AS cpu_lim,\n    argMaxIf(Value, TimeUnix, MetricName = 'k8s.container.cpu_request_utilization') AS cpu_req,\n    argMaxIf(Value, TimeUnix, MetricName = 'k8s.container.memory_limit_utilization') AS mem_lim,\n    argMaxIf(Value, TimeUnix, MetricName = 'k8s.container.memory_request_utilization') AS mem_req\n  FROM {{METRICS_SCHEMA}}.otel_metrics_gauge\n  WHERE TimeUnix > now() - INTERVAL 1 HOUR\n    AND MetricName IN ('k8s.container.cpu_limit_utilization', 'k8s.container.cpu_request_utilization', 'k8s.container.memory_limit_utilization', 'k8s.container.memory_request_utilization')\n    AND $__filters\n  GROUP BY ns, pod, container\n),\nu AS (\n  SELECT ResourceAttributes['k8s.namespace.name'] AS ns, ResourceAttributes['k8s.pod.name'] AS pod,\n    ResourceAttributes['k8s.container.name'] AS container, argMax(Value, TimeUnix) AS uptime\n  FROM {{METRICS_SCHEMA}}.otel_metrics_sum\n  WHERE TimeUnix > now() - INTERVAL 1 HOUR AND MetricName = 'container.uptime'\n  GROUP BY ns, pod, container\n)\nSELECT g.ns AS Namespace, g.pod AS Pod, g.container AS Container,\n  if(isNaN(g.cpu_lim), '-', concat(toString(round(g.cpu_lim * 100, 2)), '%')) AS \"CPU/limit\",\n  if(isNaN(g.cpu_req), '-', concat(toString(round(g.cpu_req * 100, 2)), '%')) AS \"CPU/request\",\n  if(isNaN(g.mem_lim), '-', concat(toString(round(g.mem_lim * 100, 2)), '%')) AS \"Mem/limit\",\n  if(isNaN(g.mem_req), '-', concat(toString(round(g.mem_req * 100, 2)), '%')) AS \"Mem/request\",\n  if(u.uptime > 0, formatReadableTimeDelta(toUInt64(u.uptime)), '-') AS Uptime\nFROM g LEFT JOIN u USING (ns, pod, container)\nORDER BY g.cpu_lim DESC\nLIMIT 100"
      }
    },
    {
      "name": "",
      "x": 0,
      "y": 116,
      "w": 24,
      "h": 6,
      "config": {
        "displayType": "markdown",
        "markdown": "## 5. Cluster Inventory\nDeployed nodes, namespaces, workloads, pods, and containers observed in the selected time range."
      }
    },
    {
      "name": "Observed namespaces",
      "x": 0,
      "y": 122,
      "w": 6,
      "h": 5,
      "config": {
        "displayType": "number",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT uniqExact(ResourceAttributes['k8s.namespace.name']) AS \"Namespaces\" FROM {{METRICS_SCHEMA}}.otel_metrics_gauge WHERE TimeUnix >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64}) AND TimeUnix <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64}) AND MetricName = 'k8s.pod.phase' AND $__filters",
        "numberFormat": {
          "output": "number",
          "mantissa": 0
        }
      }
    },
    {
      "name": "Observed pods",
      "x": 6,
      "y": 122,
      "w": 6,
      "h": 5,
      "config": {
        "displayType": "number",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT uniqExact(tuple(ResourceAttributes['k8s.namespace.name'], ResourceAttributes['k8s.pod.name'])) AS \"Pods\" FROM {{METRICS_SCHEMA}}.otel_metrics_gauge WHERE TimeUnix >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64}) AND TimeUnix <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64}) AND MetricName = 'k8s.pod.phase' AND $__filters",
        "numberFormat": {
          "output": "number",
          "mantissa": 0
        }
      }
    },
    {
      "name": "Observed containers",
      "x": 12,
      "y": 122,
      "w": 6,
      "h": 5,
      "config": {
        "displayType": "number",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT uniqExact(tuple(ResourceAttributes['k8s.namespace.name'], ResourceAttributes['k8s.pod.name'], ResourceAttributes['k8s.container.name'])) AS \"Containers\" FROM {{METRICS_SCHEMA}}.otel_metrics_gauge WHERE TimeUnix >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64}) AND TimeUnix <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64}) AND MetricName = 'k8s.container.restarts' AND $__filters",
        "numberFormat": {
          "output": "number",
          "mantissa": 0
        }
      }
    },
    {
      "name": "Observed deployments",
      "x": 18,
      "y": 122,
      "w": 6,
      "h": 5,
      "config": {
        "displayType": "number",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT uniqExact(tuple(ResourceAttributes['k8s.namespace.name'], ResourceAttributes['k8s.deployment.name'])) AS \"Deployments\" FROM {{METRICS_SCHEMA}}.otel_metrics_gauge WHERE TimeUnix >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64}) AND TimeUnix <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64}) AND MetricName IN ('k8s.deployment.available','k8s.deployment.desired') AND $__filters",
        "numberFormat": {
          "output": "number",
          "mantissa": 0
        }
      }
    },
    {
      "name": "",
      "x": 0,
      "y": 128,
      "w": 24,
      "h": 6,
      "config": {
        "displayType": "markdown",
        "markdown": "## 6. Events & Issues\nCentral Kubernetes troubleshooting view for warning/critical events, top reasons, recent events, and impacted resources."
      }
    },
    {
      "name": "Recent Kubernetes warning events",
      "x": 0,
      "y": 134,
      "w": 24,
      "h": 8,
      "config": {
        "displayType": "table",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{LOGS_SOURCE_ID}}",
        "sqlTemplate": "SELECT Timestamp, JSONExtractString(Body, 'object', 'reason') AS Reason, JSONExtractString(Body, 'object', 'regarding', 'namespace') AS Namespace, concat(JSONExtractString(Body, 'object', 'regarding', 'kind'), ' ', JSONExtractString(Body, 'object', 'regarding', 'name')) AS Resource, substring(JSONExtractString(Body, 'object', 'note'), 1, 240) AS Message FROM {{LOGS_SCHEMA}}.{{LOGS_TABLE}} WHERE Timestamp >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64}) AND Timestamp <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64}) AND ScopeName LIKE '%k8sobjectsreceiver%' AND JSONExtractString(Body, 'object', 'type') = 'Warning' AND $__filters ORDER BY Timestamp DESC LIMIT 100"
      }
    },
    {
      "name": "Top Kubernetes event reasons / impacted resources",
      "x": 0,
      "y": 142,
      "w": 24,
      "h": 7,
      "config": {
        "displayType": "table",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{LOGS_SOURCE_ID}}",
        "sqlTemplate": "SELECT JSONExtractString(Body, 'object', 'reason') AS Reason, JSONExtractString(Body, 'object', 'regarding', 'namespace') AS Namespace, count() AS Events, uniqExact(concat(JSONExtractString(Body, 'object', 'regarding', 'kind'), '/', JSONExtractString(Body, 'object', 'regarding', 'name'))) AS \"Impacted resources\", max(Timestamp) AS \"Last seen\" FROM {{LOGS_SCHEMA}}.{{LOGS_TABLE}} WHERE Timestamp >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64}) AND Timestamp <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64}) AND ScopeName LIKE '%k8sobjectsreceiver%' AND $__filters GROUP BY Reason, Namespace ORDER BY Events DESC LIMIT 50"
      }
    }
  ]
}
