{
  "name": "ClickStack - ClickHouse - Storage & MergeTree",
  "tags": [
    "clickstack-templates",
    "clickhouse",
    "tmpl:ch-storage"
  ],
  "filters": [],
  "tiles": [
    {
      "name": "",
      "x": 0,
      "y": 0,
      "w": 24,
      "h": 3,
      "config": {
        "displayType": "markdown",
        "markdown": "#### Storage — at a glance"
      }
    },
    {
      "name": "Disk used (active parts)",
      "x": 0,
      "y": 3,
      "w": 6,
      "h": 4,
      "config": {
        "displayType": "number",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "numberFormat": {
          "output": "byte",
          "numericUnit": "bytes_iec"
        },
        "sqlTemplate": "SELECT sum(bytes_on_disk) FROM system.parts WHERE active"
      }
    },
    {
      "name": "Compression ratio (uncompressed / compressed)",
      "x": 6,
      "y": 3,
      "w": 6,
      "h": 4,
      "config": {
        "displayType": "number",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT round(sum(data_uncompressed_bytes) / nullIf(sum(data_compressed_bytes), 0), 2) FROM system.parts WHERE active"
      }
    },
    {
      "name": "Active parts (total)",
      "x": 12,
      "y": 3,
      "w": 6,
      "h": 4,
      "config": {
        "displayType": "number",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT count() FROM system.parts WHERE active"
      }
    },
    {
      "name": "Rows stored (active)",
      "x": 18,
      "y": 3,
      "w": 6,
      "h": 4,
      "config": {
        "displayType": "number",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT sum(rows) FROM system.parts WHERE active"
      }
    },
    {
      "name": "",
      "x": 0,
      "y": 7,
      "w": 24,
      "h": 3,
      "config": {
        "displayType": "markdown",
        "markdown": "#### Throughput & merges"
      }
    },
    {
      "name": "Part events / interval (inserts, merges, mutations)",
      "x": 0,
      "y": 10,
      "w": 12,
      "h": 6,
      "config": {
        "displayType": "stacked_bar",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT toStartOfInterval(event_time, INTERVAL {intervalSeconds:Int64} SECOND) AS t,\n       countIf(event_type = 'NewPart') AS new_parts,\n       countIf(event_type = 'MergeParts') AS merges,\n       countIf(event_type = 'MutatePart') AS mutations\nFROM system.part_log\nWHERE event_time >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64})\n  AND event_time <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64})\nGROUP BY t\nORDER BY t"
      }
    },
    {
      "name": "Merge duration - p95 / max",
      "x": 12,
      "y": 10,
      "w": 12,
      "h": 6,
      "config": {
        "displayType": "line",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "numberFormat": {
          "output": "duration"
        },
        "sqlTemplate": "SELECT toStartOfInterval(event_time, INTERVAL {intervalSeconds:Int64} SECOND) AS t,\n       quantile(0.95)(duration_ms) / 1000 AS p95,\n       max(duration_ms) / 1000 AS max\nFROM system.part_log\nWHERE event_type = 'MergeParts'\n  AND event_time >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64})\n  AND event_time <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64})\nGROUP BY t\nORDER BY t"
      }
    },
    {
      "name": "Bytes written - inserted vs merged / interval",
      "x": 0,
      "y": 16,
      "w": 12,
      "h": 6,
      "config": {
        "displayType": "line",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "numberFormat": {
          "output": "byte",
          "numericUnit": "bytes_iec"
        },
        "sqlTemplate": "SELECT toStartOfInterval(event_time, INTERVAL {intervalSeconds:Int64} SECOND) AS t,\n       sumIf(size_in_bytes, event_type = 'NewPart') AS inserted_bytes,\n       sumIf(size_in_bytes, event_type = 'MergeParts') AS merged_bytes\nFROM system.part_log\nWHERE event_time >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64})\n  AND event_time <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64})\nGROUP BY t\nORDER BY t"
      }
    },
    {
      "name": "Rows processed - inserted vs merged / interval",
      "x": 12,
      "y": 16,
      "w": 12,
      "h": 6,
      "config": {
        "displayType": "line",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT toStartOfInterval(event_time, INTERVAL {intervalSeconds:Int64} SECOND) AS t,\n       sumIf(rows, event_type = 'NewPart') AS inserted_rows,\n       sumIf(rows, event_type = 'MergeParts') AS merged_rows\nFROM system.part_log\nWHERE event_time >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64})\n  AND event_time <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64})\nGROUP BY t\nORDER BY t"
      }
    },
    {
      "name": "",
      "x": 0,
      "y": 22,
      "w": 24,
      "h": 3,
      "config": {
        "displayType": "markdown",
        "markdown": "#### Tables & parts"
      }
    },
    {
      "name": "Largest tables by disk",
      "x": 0,
      "y": 25,
      "w": 24,
      "h": 6,
      "config": {
        "displayType": "table",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT database,\n       table,\n       formatReadableSize(sum(bytes_on_disk)) AS disk,\n       sum(rows) AS rows,\n       count() AS parts,\n       round(sum(data_uncompressed_bytes) / nullIf(sum(data_compressed_bytes), 0), 2) AS compression\nFROM system.parts\nWHERE active\nGROUP BY database, table\nORDER BY sum(bytes_on_disk) DESC\nLIMIT 30"
      }
    },
    {
      "name": "Active parts per table (too-many-parts watch)",
      "x": 0,
      "y": 31,
      "w": 24,
      "h": 6,
      "config": {
        "displayType": "table",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT database,\n       table,\n       count() AS active_parts,\n       sum(marks) AS marks,\n       any(part_type) AS part_type,\n       formatReadableSize(avg(bytes_on_disk)) AS avg_part_size\nFROM system.parts\nWHERE active\nGROUP BY database, table\nORDER BY active_parts DESC\nLIMIT 30"
      }
    },
    {
      "name": "Recent merges",
      "x": 0,
      "y": 37,
      "w": 24,
      "h": 6,
      "config": {
        "displayType": "table",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT event_time,\n       database || '.' || table AS tbl,\n       duration_ms,\n       rows,\n       formatReadableSize(size_in_bytes) AS size,\n       merge_reason,\n       if(error = 0, 'ok', toString(error)) AS status\nFROM system.part_log\nWHERE event_type = 'MergeParts'\n  AND event_time >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64})\n  AND event_time <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64})\nORDER BY event_time DESC\nLIMIT 30"
      }
    },
    {
      "name": "Disk free %",
      "x": 0,
      "y": 43,
      "w": 8,
      "h": 4,
      "config": {
        "displayType": "number",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT min(round(100 * free_space / nullIf(total_space, 0), 1)) AS \"Disk free %\"\nFROM system.disks"
      }
    },
    {
      "name": "Active merges",
      "x": 8,
      "y": 43,
      "w": 8,
      "h": 4,
      "config": {
        "displayType": "number",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT count() AS \"Active merges\" FROM system.merges"
      }
    },
    {
      "name": "Pending mutations",
      "x": 16,
      "y": 43,
      "w": 8,
      "h": 4,
      "config": {
        "displayType": "number",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT count() AS \"Pending mutations\" FROM system.mutations WHERE is_done = 0"
      }
    },
    {
      "name": "Disk free vs used",
      "x": 0,
      "y": 47,
      "w": 24,
      "h": 6,
      "config": {
        "displayType": "table",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT name,\n       formatReadableSize(free_space) AS free,\n       formatReadableSize(total_space - free_space) AS used,\n       formatReadableSize(total_space) AS total,\n       round(100 * free_space / nullIf(total_space, 0), 1) AS free_pct\nFROM system.disks\nORDER BY free_pct ASC"
      }
    },
    {
      "name": "Current merges",
      "x": 0,
      "y": 53,
      "w": 24,
      "h": 6,
      "config": {
        "displayType": "table",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT database,\n       table,\n       elapsed,\n       progress,\n       num_parts,\n       formatReadableSize(total_size_bytes_compressed) AS total_size_compressed\nFROM system.merges\nORDER BY elapsed DESC"
      }
    },
    {
      "name": "Active mutations",
      "x": 0,
      "y": 59,
      "w": 24,
      "h": 6,
      "config": {
        "displayType": "table",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{METRICS_SOURCE_ID}}",
        "sqlTemplate": "SELECT database,\n       table,\n       mutation_id,\n       command,\n       parts_to_do,\n       is_done\nFROM system.mutations\nWHERE is_done = 0\nORDER BY parts_to_do DESC"
      }
    }
  ]
}
