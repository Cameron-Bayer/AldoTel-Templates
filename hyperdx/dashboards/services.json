{
  "name": "ClickStack - Services (RED)",
  "tags": [
    "clickstack-templates",
    "services",
    "tmpl:services",
    "data-plane"
  ],
  "filters": [
    {
      "type": "QUERY_EXPRESSION",
      "name": "Service",
      "expression": "ServiceName",
      "sourceId": "{{TRACES_SOURCE_ID}}",
      "whereLanguage": "sql"
    }
  ],
  "tiles": [
    {
      "name": "",
      "x": 0,
      "y": 0,
      "w": 24,
      "h": 6,
      "config": {
        "displayType": "markdown",
        "markdown": "### Traces & Service Reliability\nService overview, latency analysis, RED request health, end-to-end trace investigation, dependencies, error correlation, and SLO reliability. Data comes from connected OpenTelemetry spans; dependency and request-journey results improve as applications propagate trace context and populate peer/service attributes."
      }
    },
    {
      "name": "",
      "x": 0,
      "y": 6,
      "w": 24,
      "h": 5,
      "config": {
        "displayType": "markdown",
        "markdown": "#### Rate & errors\nServer request throughput and error percentage per service, from OpenTelemetry server spans."
      }
    },
    {
      "name": "Server request count over time, by service",
      "x": 0,
      "y": 11,
      "w": 12,
      "h": 6,
      "config": {
        "displayType": "line",
        "sourceId": "{{TRACES_SOURCE_ID}}",
        "groupBy": "ServiceName",
        "select": [
          {
            "aggFn": "count",
            "alias": "requests",
            "where": "SpanKind:Server",
            "whereLanguage": "lucene"
          }
        ]
      }
    },
    {
      "name": "Error rate %",
      "x": 12,
      "y": 11,
      "w": 12,
      "h": 6,
      "config": {
        "displayType": "line",
        "sourceId": "{{TRACES_SOURCE_ID}}",
        "groupBy": "ServiceName",
        "asRatio": true,
        "numberFormat": {
          "output": "percent",
          "mantissa": 3
        },
        "select": [
          {
            "aggFn": "count",
            "alias": "errors",
            "where": "SpanKind:Server AND StatusCode:Error",
            "whereLanguage": "lucene"
          },
          {
            "aggFn": "count",
            "alias": "total",
            "where": "SpanKind:Server",
            "whereLanguage": "lucene"
          }
        ]
      }
    },
    {
      "name": "",
      "x": 0,
      "y": 17,
      "w": 24,
      "h": 5,
      "config": {
        "displayType": "markdown",
        "markdown": "#### Latency & error breakdown\n**p50 / p95 / p99** are latency percentiles: 50%, 95%, and 99% of requests complete within that time. A widening gap between p50 and p99 indicates worsening tail latency."
      }
    },
    {
      "name": "Server latency percentiles (p50 / p95 / p99)",
      "x": 0,
      "y": 22,
      "w": 12,
      "h": 6,
      "config": {
        "displayType": "line",
        "sourceId": "{{TRACES_SOURCE_ID}}",
        "select": [
          {
            "aggFn": "quantile",
            "level": 0.5,
            "valueExpression": "Duration / 1000000000",
            "alias": "p50",
            "where": "SpanKind = 'Server'",
            "whereLanguage": "sql"
          },
          {
            "aggFn": "quantile",
            "level": 0.95,
            "valueExpression": "Duration / 1000000000",
            "alias": "p95",
            "where": "SpanKind = 'Server'",
            "whereLanguage": "sql"
          },
          {
            "aggFn": "quantile",
            "level": 0.99,
            "valueExpression": "Duration / 1000000000",
            "alias": "p99",
            "where": "SpanKind = 'Server'",
            "whereLanguage": "sql"
          }
        ],
        "numberFormat": {
          "output": "duration"
        }
      }
    },
    {
      "name": "Errors by status message",
      "x": 12,
      "y": 22,
      "w": 12,
      "h": 6,
      "config": {
        "displayType": "pie",
        "sourceId": "{{TRACES_SOURCE_ID}}",
        "groupBy": "StatusMessage",
        "select": [
          {
            "aggFn": "count",
            "alias": "errors",
            "where": "SpanKind:Server AND StatusCode:Error",
            "whereLanguage": "lucene"
          }
        ]
      }
    },
    {
      "name": "",
      "x": 0,
      "y": 28,
      "w": 24,
      "h": 6,
      "config": {
        "displayType": "markdown",
        "markdown": "#### Slow routes & distribution\nSlowest HTTP routes by p95, plus a latency anomaly detector and a distribution heatmap. The anomaly chart compares recent p95 against a rolling baseline; the shaded ±3σ band is three standard deviations around that baseline, so points above it are unusually slow. That chart uses a fixed last-24h window (against an 8-day baseline) and ignores the time picker."
      }
    },
    {
      "name": "Slowest routes (p95) - min 20 requests",
      "x": 0,
      "y": 34,
      "w": 24,
      "h": 6,
      "config": {
        "displayType": "table",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{TRACES_SOURCE_ID}}",
        "sqlTemplate": "SELECT ServiceName AS \"Service\", route AS \"Route\", round(p95_ms, 1) AS \"p95 (ms)\", round(p50_ms, 1) AS \"p50 (ms)\", requests AS \"Requests\" FROM (\n  SELECT ServiceName,\n         SpanAttributes['http.route'] AS route,\n         quantile(0.95)(Duration) / 1e6 AS p95_ms,\n         quantile(0.5)(Duration) / 1e6 AS p50_ms,\n         count() AS requests\n  FROM {{TRACES_SCHEMA}}.{{TRACES_TABLE}}\n  WHERE SpanKind = 'Server'\n    AND Timestamp >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64})\n    AND Timestamp <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64})\n    AND SpanAttributes['http.route'] != ''\n    AND $__filters\n  GROUP BY ServiceName, route\n  HAVING requests >= 20\n)\nORDER BY p95_ms DESC\nLIMIT 50"
      }
    },
    {
      "name": "P95 latency anomaly — last 24h vs 8-day baseline (±3σ band)",
      "x": 0,
      "y": 40,
      "w": 24,
      "h": 7,
      "config": {
        "displayType": "line",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{TRACES_SOURCE_ID}}",
        "sqlTemplate": "WITH points AS (\n  SELECT toStartOfInterval(Timestamp, INTERVAL 5 MINUTE) AS t,\n         quantile(0.95)(Duration)/1e6 AS p95_ms\n  FROM {{TRACES_SCHEMA}}.{{TRACES_TABLE}}\n  WHERE SpanKind = 'Server' AND Timestamp > now() - INTERVAL 8 DAY AND $__filters\n  GROUP BY t\n),\nscored AS (\n  SELECT t, p95_ms,\n         avg(p95_ms)       OVER (ORDER BY t ROWS BETWEEN 288 PRECEDING AND 12 PRECEDING) AS base,\n         stddevPop(p95_ms) OVER (ORDER BY t ROWS BETWEEN 288 PRECEDING AND 12 PRECEDING) AS sigma\n  FROM points\n)\nSELECT t,\n       p95_ms,\n       base AS baseline_ms,\n       base + 3 * sigma AS upper_ms,\n       greatest(base - 3 * sigma, 0) AS lower_ms\nFROM scored\nWHERE t >= now() - INTERVAL 24 HOUR\nORDER BY t"
      }
    },
    {
      "name": "Server latency distribution (heatmap, seconds)",
      "x": 0,
      "y": 47,
      "w": 24,
      "h": 7,
      "config": {
        "displayType": "heatmap",
        "sourceId": "{{TRACES_SOURCE_ID}}",
        "where": "SpanKind:Server",
        "whereLanguage": "lucene",
        "numberFormat": {
          "output": "duration"
        },
        "select": [
          {
            "valueExpression": "Duration / 1000000000",
            "countExpression": "count()",
            "heatmapScaleType": "log",
            "where": "",
            "whereLanguage": "sql"
          }
        ]
      }
    },
    {
      "name": "",
      "x": 0,
      "y": 54,
      "w": 24,
      "h": 6,
      "config": {
        "displayType": "markdown",
        "markdown": "#### SLO & error budget\nAn **SLO** (service-level objective) is your reliability target; the **SLI** (service-level indicator) is the measured value. Availability = successful server requests ÷ total server requests. Burn-rate windows (1h / 6h / 24h / 3d) are **fixed SLO windows** and intentionally ignore the time picker; a burn rate > 1 means the 99.9% error budget is being spent faster than sustainable."
      }
    },
    {
      "name": "Availability (SLI = success rate)",
      "x": 0,
      "y": 60,
      "w": 12,
      "h": 4,
      "config": {
        "displayType": "number",
        "sourceId": "{{TRACES_SOURCE_ID}}",
        "numberFormat": {
          "output": "percent",
          "mantissa": 3
        },
        "select": [
          {
            "aggFn": "avg",
            "valueExpression": "if(StatusCode = 'Error', 0, 1)",
            "alias": "availability",
            "where": "SpanKind:Server",
            "whereLanguage": "lucene"
          }
        ],
        "colorRules": [
          {
            "operator": "lt",
            "value": 0.999,
            "color": "chart-warning",
            "label": "Below 99.9%"
          },
          {
            "operator": "lt",
            "value": 0.995,
            "color": "chart-error",
            "label": "Below 99.5%"
          }
        ]
      }
    },
    {
      "name": "Error budget remaining (window, SLO 99.9%)",
      "x": 12,
      "y": 60,
      "w": 12,
      "h": 4,
      "config": {
        "displayType": "number",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{TRACES_SOURCE_ID}}",
        "numberFormat": {
          "output": "percent",
          "mantissa": 1
        },
        "sqlTemplate": "SELECT 1 - (countIf(SpanKind = 'Server' AND StatusCode = 'Error') / nullIf(countIf(SpanKind = 'Server'), 0)) / 0.001 AS \"Budget remaining\"\nFROM {{TRACES_SCHEMA}}.{{TRACES_TABLE}}\nWHERE Timestamp >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64})\n  AND Timestamp <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64})\n  AND $__filters",
        "colorRules": [
          {
            "operator": "lt",
            "value": 0.25,
            "color": "chart-warning",
            "label": "Budget low"
          },
          {
            "operator": "lt",
            "value": 0,
            "color": "chart-error",
            "label": "Budget exhausted"
          }
        ]
      }
    },
    {
      "name": "Multi-window burn rate (SLO 99.9%)",
      "x": 0,
      "y": 64,
      "w": 12,
      "h": 6,
      "config": {
        "displayType": "table",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{TRACES_SOURCE_ID}}",
        "sqlTemplate": "WITH 0.001 AS budget\nSELECT window AS \"Window\",\n       round(error_ratio, 5) AS \"Error rate\",\n       round(error_ratio / budget, 2) AS \"Burn rate (×)\"\nFROM (\n  SELECT '1h' AS window, 1 AS ord,\n         countIf(SpanKind = 'Server' AND StatusCode = 'Error') / nullIf(countIf(SpanKind = 'Server'), 0) AS error_ratio\n  FROM {{TRACES_SCHEMA}}.{{TRACES_TABLE}} WHERE Timestamp > now() - INTERVAL 1 HOUR AND $__filters\n  UNION ALL\n  SELECT '6h', 2,\n         countIf(SpanKind = 'Server' AND StatusCode = 'Error') / nullIf(countIf(SpanKind = 'Server'), 0)\n  FROM {{TRACES_SCHEMA}}.{{TRACES_TABLE}} WHERE Timestamp > now() - INTERVAL 6 HOUR AND $__filters\n  UNION ALL\n  SELECT '24h', 3,\n         countIf(SpanKind = 'Server' AND StatusCode = 'Error') / nullIf(countIf(SpanKind = 'Server'), 0)\n  FROM {{TRACES_SCHEMA}}.{{TRACES_TABLE}} WHERE Timestamp > now() - INTERVAL 24 HOUR AND $__filters\n  UNION ALL\n  SELECT '3d', 4,\n         countIf(SpanKind = 'Server' AND StatusCode = 'Error') / nullIf(countIf(SpanKind = 'Server'), 0)\n  FROM {{TRACES_SCHEMA}}.{{TRACES_TABLE}} WHERE Timestamp > now() - INTERVAL 3 DAY AND $__filters\n)\nORDER BY ord"
      }
    },
    {
      "name": "Availability over time (target 99.9%)",
      "x": 12,
      "y": 64,
      "w": 12,
      "h": 6,
      "config": {
        "displayType": "line",
        "sourceId": "{{TRACES_SOURCE_ID}}",
        "asRatio": true,
        "numberFormat": {
          "output": "percent",
          "mantissa": 3
        },
        "select": [
          {
            "aggFn": "count",
            "alias": "good",
            "where": "SpanKind:Server AND NOT StatusCode:Error",
            "whereLanguage": "lucene"
          },
          {
            "aggFn": "count",
            "alias": "total",
            "where": "SpanKind:Server",
            "whereLanguage": "lucene"
          }
        ]
      }
    },
    {
      "name": "",
      "x": 0,
      "y": 70,
      "w": 24,
      "h": 5,
      "config": {
        "displayType": "markdown",
        "markdown": "#### 1. Service Overview\nHigh-level health of all monitored services: request volume, reliability, latency, and the most impacted services."
      }
    },
    {
      "name": "Server requests (selected range)",
      "x": 0,
      "y": 75,
      "w": 6,
      "h": 5,
      "config": {
        "displayType": "number",
        "sourceId": "{{TRACES_SOURCE_ID}}",
        "select": [
          {
            "aggFn": "count",
            "alias": "requests",
            "where": "SpanKind:Server",
            "whereLanguage": "lucene"
          }
        ]
      }
    },
    {
      "name": "Success rate",
      "x": 6,
      "y": 75,
      "w": 6,
      "h": 5,
      "config": {
        "displayType": "number",
        "sourceId": "{{TRACES_SOURCE_ID}}",
        "select": [
          {
            "aggFn": "avg",
            "valueExpression": "if(StatusCode = 'Error', 0, 1)",
            "alias": "success",
            "where": "SpanKind:Server",
            "whereLanguage": "lucene"
          }
        ],
        "numberFormat": {
          "output": "percent",
          "mantissa": 3
        }
      }
    },
    {
      "name": "Average server latency",
      "x": 12,
      "y": 75,
      "w": 6,
      "h": 5,
      "config": {
        "displayType": "number",
        "sourceId": "{{TRACES_SOURCE_ID}}",
        "select": [
          {
            "aggFn": "avg",
            "valueExpression": "Duration / 1000000000",
            "alias": "latency",
            "where": "SpanKind = 'Server'",
            "whereLanguage": "sql"
          }
        ],
        "numberFormat": {
          "output": "duration"
        }
      }
    },
    {
      "name": "Average client latency",
      "x": 18,
      "y": 75,
      "w": 6,
      "h": 5,
      "config": {
        "displayType": "number",
        "sourceId": "{{TRACES_SOURCE_ID}}",
        "select": [
          {
            "aggFn": "avg",
            "valueExpression": "Duration / 1000000000",
            "alias": "latency",
            "where": "SpanKind = 'Client'",
            "whereLanguage": "sql"
          }
        ],
        "numberFormat": {
          "output": "duration"
        }
      }
    },
    {
      "name": "Service health summary / top impacted services",
      "x": 0,
      "y": 80,
      "w": 24,
      "h": 7,
      "config": {
        "displayType": "table",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{TRACES_SOURCE_ID}}",
        "sqlTemplate": "SELECT ServiceName AS Service, count() AS Requests, round(100 * countIf(StatusCode = 'Error') / nullIf(count(), 0), 3) AS \"Error rate %\", round(avg(Duration) / 1e6, 1) AS \"Average latency (ms)\", round(quantile(0.95)(Duration) / 1e6, 1) AS \"P95 latency (ms)\", greatest(0, round(100 - \"Error rate %\" * 10 - least(\"P95 latency (ms)\" / 100, 30), 0)) AS \"Health score\", if(\"Error rate %\" >= 5 OR \"P95 latency (ms)\" >= 2000, 'Critical', if(\"Error rate %\" >= 1 OR \"P95 latency (ms)\" >= 1000, 'Warning', 'Healthy')) AS Health FROM {{TRACES_SCHEMA}}.{{TRACES_TABLE}} WHERE Timestamp >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64}) AND Timestamp <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64}) AND SpanKind = 'Server' AND $__filters GROUP BY ServiceName ORDER BY Health ASC, \"Error rate %\" DESC, \"P95 latency (ms)\" DESC"
      }
    },
    {
      "name": "",
      "x": 0,
      "y": 87,
      "w": 24,
      "h": 5,
      "config": {
        "displayType": "markdown",
        "markdown": "#### 2. Latency Analysis\nServer, client, and RPC latency by service and operation, latency percentiles, slow routes, distribution, and anomaly detection."
      }
    },
    {
      "name": "HTTP server / client and RPC latency by service",
      "x": 0,
      "y": 92,
      "w": 24,
      "h": 7,
      "config": {
        "displayType": "table",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{TRACES_SOURCE_ID}}",
        "sqlTemplate": "SELECT ServiceName AS Service, SpanKind AS Kind, if(SpanAttributes['rpc.system'] != '', concat('RPC: ', SpanAttributes['rpc.system']), if(SpanAttributes['http.request.method'] != '' OR SpanAttributes['http.method'] != '', 'HTTP', 'Other')) AS Protocol, round(avg(Duration) / 1e6, 1) AS \"Average (ms)\", round(quantile(0.95)(Duration) / 1e6, 1) AS \"P95 (ms)\", count() AS Requests FROM {{TRACES_SCHEMA}}.{{TRACES_TABLE}} WHERE Timestamp >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64}) AND Timestamp <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64}) AND SpanKind IN ('Server','Client') AND $__filters GROUP BY ServiceName, SpanKind, Protocol ORDER BY \"P95 (ms)\" DESC"
      }
    },
    {
      "name": "Latency by service & operation",
      "x": 0,
      "y": 99,
      "w": 24,
      "h": 7,
      "config": {
        "displayType": "table",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{TRACES_SOURCE_ID}}",
        "sqlTemplate": "SELECT ServiceName AS Service, SpanName AS Operation, SpanKind AS Kind, round(avg(Duration) / 1e6, 1) AS \"Average (ms)\", round(quantile(0.50)(Duration) / 1e6, 1) AS \"P50 (ms)\", round(quantile(0.95)(Duration) / 1e6, 1) AS \"P95 (ms)\", round(quantile(0.99)(Duration) / 1e6, 1) AS \"P99 (ms)\", count() AS Requests FROM {{TRACES_SCHEMA}}.{{TRACES_TABLE}} WHERE Timestamp >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64}) AND Timestamp <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64}) AND SpanKind IN ('Server','Client') AND $__filters GROUP BY ServiceName, SpanName, SpanKind HAVING Requests >= 5 ORDER BY \"P95 (ms)\" DESC LIMIT 100"
      }
    },
    {
      "name": "",
      "x": 0,
      "y": 106,
      "w": 24,
      "h": 5,
      "config": {
        "displayType": "markdown",
        "markdown": "#### 3. Request Health (RED Metrics)\nRequest rate, throughput, failures, status messages, success rate, and user-impact analysis. The existing RED charts above provide the time-series view."
      }
    },
    {
      "name": "Failed request analysis",
      "x": 0,
      "y": 111,
      "w": 24,
      "h": 7,
      "config": {
        "displayType": "table",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{TRACES_SOURCE_ID}}",
        "sqlTemplate": "SELECT ServiceName AS Service, SpanName AS Operation, StatusMessage AS Error, count() AS Failures, round(quantile(0.95)(Duration) / 1e6, 1) AS \"P95 (ms)\", max(Timestamp) AS \"Last seen\" FROM {{TRACES_SCHEMA}}.{{TRACES_TABLE}} WHERE Timestamp >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64}) AND Timestamp <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64}) AND SpanKind = 'Server' AND StatusCode = 'Error' AND $__filters GROUP BY ServiceName, SpanName, StatusMessage ORDER BY Failures DESC LIMIT 100"
      }
    },
    {
      "name": "",
      "x": 0,
      "y": 118,
      "w": 24,
      "h": 5,
      "config": {
        "displayType": "markdown",
        "markdown": "#### 4. End-to-End Request Tracing\nSearch distributed traces, open waterfalls, inspect critical paths, and isolate slow or failed requests. Click a span to open its full trace journey in HyperDX."
      }
    },
    {
      "name": "Slow and failed request traces",
      "x": 0,
      "y": 123,
      "w": 24,
      "h": 8,
      "config": {
        "displayType": "search",
        "sourceId": "{{TRACES_SOURCE_ID}}",
        "select": "Timestamp, ServiceName, SpanName, Duration, StatusCode, StatusMessage, TraceId, SpanId, ParentSpanId",
        "where": "SpanKind = 'Server' AND (StatusCode = 'Error' OR Duration >= 1000000000)",
        "whereLanguage": "sql"
      }
    },
    {
      "name": "Trace sampling analytics",
      "x": 0,
      "y": 131,
      "w": 24,
      "h": 7,
      "config": {
        "displayType": "table",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{TRACES_SOURCE_ID}}",
        "sqlTemplate": "SELECT ServiceName AS Service, uniqExact(TraceId) AS Traces, count() AS Spans, round(count() / nullIf(uniqExact(TraceId), 0), 1) AS \"Spans / trace\", round(quantile(0.95)(Duration) / 1e6, 1) AS \"P95 span (ms)\" FROM {{TRACES_SCHEMA}}.{{TRACES_TABLE}} WHERE Timestamp >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64}) AND Timestamp <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64}) AND $__filters GROUP BY ServiceName ORDER BY Traces DESC"
      }
    },
    {
      "name": "",
      "x": 0,
      "y": 138,
      "w": 24,
      "h": 5,
      "config": {
        "displayType": "markdown",
        "markdown": "#### 5. Service Dependency Mapping\nUpstream/downstream request relationships inferred from client spans and peer attributes, including dependency latency and errors."
      }
    },
    {
      "name": "Service dependency map / most impacted dependencies",
      "x": 0,
      "y": 143,
      "w": 24,
      "h": 7,
      "config": {
        "displayType": "table",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{TRACES_SOURCE_ID}}",
        "sqlTemplate": "SELECT ServiceName AS Upstream, coalesce(nullIf(SpanAttributes['peer.service'], ''), nullIf(SpanAttributes['server.address'], ''), nullIf(SpanAttributes['net.peer.name'], ''), SpanName) AS Downstream, count() AS Requests, round(100 * countIf(StatusCode = 'Error') / nullIf(count(), 0), 3) AS \"Error rate %\", round(quantile(0.95)(Duration) / 1e6, 1) AS \"P95 latency (ms)\" FROM {{TRACES_SCHEMA}}.{{TRACES_TABLE}} WHERE Timestamp >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64}) AND Timestamp <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64}) AND SpanKind = 'Client' AND $__filters GROUP BY Upstream, Downstream ORDER BY \"Error rate %\" DESC, \"P95 latency (ms)\" DESC"
      }
    },
    {
      "name": "",
      "x": 0,
      "y": 150,
      "w": 24,
      "h": 5,
      "config": {
        "displayType": "markdown",
        "markdown": "#### 6. Error Correlation & Root Cause Analysis\nCorrelate failures across services and time. Candidate root causes are ranked by earliest errors and error concentration; confirm them in the trace waterfall and correlated logs."
      }
    },
    {
      "name": "Root cause candidate services / error propagation",
      "x": 0,
      "y": 155,
      "w": 24,
      "h": 7,
      "config": {
        "displayType": "table",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{TRACES_SOURCE_ID}}",
        "sqlTemplate": "SELECT TraceId, argMin(ServiceName, Timestamp) AS \"First error service\", min(Timestamp) AS \"First error\", groupUniqArray(ServiceName) AS \"Impacted services\", count() AS \"Error spans\" FROM {{TRACES_SCHEMA}}.{{TRACES_TABLE}} WHERE Timestamp >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64}) AND Timestamp <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64}) AND StatusCode = 'Error' AND $__filters GROUP BY TraceId HAVING \"Error spans\" > 0 ORDER BY \"Error spans\" DESC, \"First error\" DESC LIMIT 100"
      }
    },
    {
      "name": "",
      "x": 0,
      "y": 162,
      "w": 24,
      "h": 5,
      "config": {
        "displayType": "markdown",
        "markdown": "#### 7. SLO & Reliability\nAvailability, error budget remaining, burn rate, compliance trend, and violations are implemented by the SLO strip above."
      }
    },
    {
      "name": "",
      "x": 0,
      "y": 167,
      "w": 24,
      "h": 5,
      "config": {
        "displayType": "markdown",
        "markdown": "#### 8. Infrastructure & Platform Performance\nUse **Infrastructure** for compute/storage/network, **Kubernetes** for nodes/workloads, and advanced **Observability Platform Health** for ClickHouse query/storage, Keeper-adjacent server health, collector health, and the OTel pipeline."
      }
    },
    {
      "name": "SLO compliance dashboard / violations",
      "x": 0,
      "y": 172,
      "w": 24,
      "h": 7,
      "config": {
        "displayType": "table",
        "configType": "sql",
        "connectionId": "{{CONNECTION_ID}}",
        "sourceId": "{{TRACES_SOURCE_ID}}",
        "sqlTemplate": "SELECT ServiceName AS Service, round(100 * countIf(StatusCode != 'Error') / nullIf(count(), 0), 3) AS \"Availability %\", 99.9 AS \"SLO target %\", round(100 * (1 - (countIf(StatusCode = 'Error') / nullIf(count(), 0)) / 0.001), 1) AS \"Error budget remaining %\", if(\"Availability %\" >= 99.9, 'Compliant', 'Violation') AS Status FROM {{TRACES_SCHEMA}}.{{TRACES_TABLE}} WHERE Timestamp >= fromUnixTimestamp64Milli({startDateMilliseconds:Int64}) AND Timestamp <= fromUnixTimestamp64Milli({endDateMilliseconds:Int64}) AND SpanKind = 'Server' AND $__filters GROUP BY ServiceName ORDER BY Status DESC, \"Availability %\""
      }
    }
  ]
}
